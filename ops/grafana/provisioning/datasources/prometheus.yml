# provisioning: единственный datasource — Prometheus из этого же stack'а.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

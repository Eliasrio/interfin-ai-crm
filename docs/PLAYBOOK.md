# Interfin AI-CRM — готовые команды для Claude Code

Копируйте по порядку. Для каждого эпика — **пять сообщений**:
① ветка → ② работа → ③ критерии → ④ правила → ⑤ коммит.

**Правило:** один эпик = один новый чат во вкладке Code. Docker Desktop запущен.
Отправляйте по одному, дожидаясь ответа Claude Code.

---

## M0 — Скелет проекта

«Создай git-ветку feat/m0-skeleton и переключись на неё.»

«Работаем над эпиком M0. Прочитай CLAUDE.md и tasks/M0_skeleton.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M0: skeleton — config, docker, migrations, health endpoints и запушь ветку на GitHub.»

---

## M1 — Слой данных

«Создай git-ветку feat/m1-data и переключись на неё.»

«Работаем над эпиком M1. Прочитай CLAUDE.md и tasks/M1_data_layer.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M1: data layer — migrations, GORM models, pgx simple protocol, schema-lint и запушь ветку на GitHub.»

---

## M2 — Telegram Ingestion

«Создай git-ветку feat/m2-ingestion и переключись на неё.»

«Работаем над эпиком M2. Прочитай CLAUDE.md и tasks/M2_telegram_ingestion.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M2: telegram ingestion — webhook, save inbound, enqueue и запушь ветку на GitHub.»

---

## M3 — Воркер + Claude

> ⚠ Перед стартом добавь ANTHROPIC_API_KEY в .env.

«Создай git-ветку feat/m3-worker и переключись на неё.»

«Работаем над эпиком M3. Прочитай CLAUDE.md и tasks/M3_worker_claude.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M3: worker + claude — dialog, token budget, dedup, retries и запушь ветку на GitHub.»

---

## M4 — RAG

> ⚠ Перед стартом добавь VOYAGE_API_KEY в .env.

«Создай git-ветку feat/m4-rag и переключись на неё.»

«Работаем над эпиком M4. Прочитай CLAUDE.md и tasks/M4_rag.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M4: rag — voyage embeddings, pgvector retrieval, summary lock и запушь ветку на GitHub.»

---

## M5 — Kanban State Machine

«Создай git-ветку feat/m5-kanban и переключись на неё.»

«Работаем над эпиком M5. Прочитай CLAUDE.md и tasks/M5_kanban_state_machine.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M5: kanban state machine — stages, ttl, anti-spam escalation и запушь ветку на GitHub.»

---

## M6 — Payment Webhook

> ⚠ Перед стартом добавь ключи крипто-шлюза в .env.

«Создай git-ветку feat/m6-payment и переключись на неё.»

«Работаем над эпиком M6. Прочитай CLAUDE.md и tasks/M6_payment.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M6: payment webhook — hmac, usdt tolerance, auto stage и запушь ветку на GitHub.»

---

## M7 — Аутентификация

> Можно вести параллельно с M2–M4.

«Создай git-ветку feat/m7-auth и переключись на неё.»

«Работаем над эпиком M7. Прочитай CLAUDE.md и tasks/M7_auth.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M7: auth — jwt rs256, refresh, roles и запушь ветку на GitHub.»

---

## M8 — REST API

«Создай git-ветку feat/m8-api и переключись на неё.»

«Работаем над эпиком M8. Прочитай CLAUDE.md и tasks/M8_rest_api.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M8: rest api — leads, catch-up, lgpd erasure/export и запушь ветку на GitHub.»

---

## M9 — WebSocket

«Создай git-ветку feat/m9-websocket и переключись на неё.»

«Работаем над эпиком M9. Прочитай CLAUDE.md и tasks/M9_websocket.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M9: websocket — hub, catch-up, heartbeat и запушь ветку на GitHub.»

---

## M10 — React Kanban

«Создай git-ветку feat/m10-frontend и переключись на неё.»

«Работаем над эпиком M10. Прочитай CLAUDE.md и tasks/M10_react_kanban.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Собери фронтенд, убедись что сборка проходит без ошибок. Затем закоммить изменения в текущую ветку с сообщением M10: react kanban — board, websocket, lgpd panel и запушь ветку на GitHub.»

---

## M11 — High Availability + Monitoring + Deploy

> ⚠ Финальный эпик: провижининг сервера и чек-лист безопасности.

«Создай git-ветку feat/m11-ops и переключись на неё.»

«Работаем над эпиком M11. Прочитай CLAUDE.md и tasks/M11_ha_monitoring.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из task-файла и покажи результаты, включая чек-лист безопасности. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку с сообщением M11: ha + monitoring + deploy — sentinel, wal, prometheus, server provisioning и запушь ветку на GitHub.»

---

## M12 — Чат менеджера и счета из CRM

> ⚠ Ключи в .env уже есть с прошлых эпиков, ничего добавлять не нужно.
> Docker Desktop запущен. Для живого смоука счёта используется тестовая
> сеть CryptoBot (тестовые монеты — кран @testgiver_ton_bot, 2 TON/час).

«Создай git-ветку feat/m12-manager-chat от ветки feat/m11-ops и переключись на неё.»

«Работаем над эпиком M12. Прочитай CLAUDE.md и tasks/M12_manager_chat.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4 и раздел "Вне скоупа" из task-файла (эти вещи делать НЕ надо), доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все проверки из раздела "Как прогнать проверки" task-файла по порядку и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./..., npm test в web/), убедись что чисто. Затем склей работу в один squash-коммит с сообщением M12: manager chat — чат из карточки, сообщения через бота, счёт cryptobot кнопкой и запушь ветку на GitHub.»

«Смерджи ветку feat/m12-manager-chat в ветку feat/m11-ops и запушь её на GitHub — боевой сервер обновляется именно с этой ветки.»

> После этого на сервере: `bash /opt/interfin-ai-crm/scripts/update_bot.sh` —
> и проверить руками: открыть карточку лида на доске, написать себе
> в Telegram сообщение из CRM, выставить себе счёт кнопкой.

---

## M13 — «Взять в работу» + напоминания об ожидающих

> ⚠ Ключи в .env уже есть, ничего добавлять не нужно. Docker Desktop запущен.
> ТЗ и решения владельца: tasks/M13_takeover.md и docs/NEXT_STEPS.md
> (тайминги 10+10 минут, интервалы — настройки в CRM).

«Создай git-ветку feat/m13-takeover от ветки feat/m11-ops и переключись на неё.»

«Работаем над эпиком M13. Прочитай CLAUDE.md и tasks/M13_takeover.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4 и раздел "Вне скоупа" из task-файла (эти вещи делать НЕ надо), доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все проверки из раздела "Как прогнать проверки" task-файла по порядку и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./..., npm test в web/), убедись что чисто. Затем склей работу в один squash-коммит с сообщением M13: takeover — взять в работу, пауза Эммы, напоминания 10+10, настройки в CRM и запушь ветку на GitHub.»

«Смерджи ветку feat/m13-takeover в ветку feat/m11-ops и запушь её на GitHub — боевой сервер обновляется именно с этой ветки.»

> После этого — деплой на сервер. ВАЖНО: у M13 есть миграции БД (0013, 0014),
> их накатываем ДО обновления приложения, иначе новый код упадёт на
> отсутствующих колонках. Порядок на сервере (ssh root@95.211.165.169):
>
> ```bash
> cd /opt/interfin-ai-crm && git pull
> docker cp migrations/. $(docker ps -qf name=crm_app | head -1):/app/migrations/
> docker exec -e POSTGRES_DSN='postgres://postgres:ПАРОЛЬ_PG_SUPER@postgres-primary:5432/interfin?sslmode=disable' $(docker ps -qf name=crm_app | head -1) /app/migrate up
> bash scripts/update_bot.sh
> ```
>
> (ПАРОЛЬ_PG_SUPER — из хранилища crmkeys; ждать «миграции применены»,
> затем «✓ сайт отвечает».)
> Проверить руками: Cmd+Shift+R на доске → карточка лида → кнопка
> «Взять в работу» (Эмма молчит, отвечаешь ты) → «Вернуть Эмме»;
> написать из карточки без кнопок — Эмма на паузе 30 минут; в режиме
> «взял и молчишь» через 10 минут придёт напоминание, ещё через 10 —
> Эмма подхватит; интервалы меняются в разделе «Настройки» (admin).

---

## M14 — Языки клиента (ru/en/es)

> ⚠ Ключи в .env уже есть, ничего добавлять не нужно. Docker Desktop запущен.
> ТЗ и решения владельца: tasks/M14_languages.md и docs/NEXT_STEPS.md
> блок 2.1 (языки ru/en/es, португальский не нужен).

«Создай git-ветку feat/m14-languages от ветки feat/m11-ops и переключись на неё.»

«Работаем над эпиком M14. Прочитай CLAUDE.md и tasks/M14_languages.md. Реализуй все задачи из файла, строго соблюдай жёсткие правила из CLAUDE.md §4 и раздел "Вне скоупа" из task-файла (эти вещи делать НЕ надо), доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все проверки из раздела "Как прогнать проверки" task-файла по порядку и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4. Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./..., npm test в web/), убедись что чисто. Затем склей работу в один squash-коммит с сообщением M14: languages — язык лида ru/en/es, Эмма отвечает на языке клиента, бейдж в карточке и запушь ветку на GitHub.»

«Смерджи ветку feat/m14-languages в ветку feat/m11-ops и запушь её на GitHub — боевой сервер обновляется именно с этой ветки.»

> После этого — деплой на сервер. ВАЖНО: у M14 есть миграция БД (0015),
> её накатываем ДО обновления приложения, иначе новый код упадёт на
> отсутствующей колонке. Порядок на сервере (ssh root@95.211.165.169):
>
> ```bash
> cd /opt/interfin-ai-crm && git pull
> docker cp migrations/. $(docker ps -qf name=crm_app | head -1):/app/migrations/
> docker exec -e POSTGRES_DSN='postgres://postgres:ПАРОЛЬ_PG_SUPER@postgres-primary:5432/interfin?sslmode=disable' $(docker ps -qf name=crm_app | head -1) /app/migrate up
> bash scripts/update_bot.sh
> ```
>
> (ПАРОЛЬ_PG_SUPER — из хранилища crmkeys; ждать «миграции применены»,
> затем «✓ сайт отвечает».)
> Проверить руками: Cmd+Shift+R на доске → написать боту с нового
> аккаунта по-английски или по-испански — на карточке появится бейдж
> EN/ES и Эмма ответит на этом языке; в модалке лида язык можно
> поменять руками; у старых лидов языка нет — это нормально, он
> появится/ставится руками.

---

## EP-01 — Панель Эммы: БД, PIN, каркас API

«Создай git-ветку feat/ep01-pin от feat/m11-ops и переключись на неё.»

«Работаем над эпиком EP-01 панели управления Эммой. Прочитай CLAUDE.md, tasks/EP01_pin.md и раздел 2 из docs/EMMA_PANEL_TZ_v2.md. Реализуй все задачи из task-файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из tasks/EP01_pin.md и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4, и что старые тесты не сломаны (go test -p 1). Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку одним squash-коммитом с сообщением EP-01: emma panel — migrations, string settings, pin auth, api skeleton и запушь ветку на GitHub.»

> Что получите после EP-01: в БД появятся все таблицы панели, у API —
> закрытая PIN-кодом группа /api/emma/* (PIN 6 цифр, установка при первом
> входе, блокировка после 5 ошибок), команда сброса забытого PIN на
> сервере. Видимого интерфейса ещё нет — он появится в EP-07, а до того
> эпики EP-02…EP-06 наполняют API. Проверить можно только запросами:
> Claude Code покажет как.

---

## EP-02 — Панель Эммы: промпт из БД + история версий

«Создай git-ветку feat/ep02-prompt от feat/m11-ops и переключись на неё.»

«Работаем над эпиком EP-02 панели управления Эммой. Прочитай CLAUDE.md, tasks/EP02_prompt.md и вкладку 1 из docs/EMMA_PANEL_TZ_v2.md (раздел 3). Реализуй все задачи из task-файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из tasks/EP02_prompt.md и покажи результаты. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4, и что старые тесты не сломаны (go test -p 1). Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку одним squash-коммитом с сообщением EP-02: emma panel — prompt from db, version history, token limit, budget 5000 и запушь ветку на GitHub.»

> Что получите после EP-02: промпт Эммы, запретные темы и стиль общения
> хранятся в базе с полной историей версий и откатом; менять их можно
> через API (интерфейс появится в EP-07). При деплое поведение Эммы не
> меняется — первая версия в базе равна текущему боевому промпту.
> Бюджет контекста поднят до 5000 токенов — каждое сообщение клиента
> станет чуть дороже (~полцента), зато влезут база знаний, контакты
> и файлы следующих эпиков.

---

## EP-03 — Панель Эммы: база знаний (загрузка → RAG)

«Создай git-ветку feat/ep03-kb от feat/m11-ops и переключись на неё.»

«Работаем над эпиком EP-03 панели управления Эммой. Прочитай CLAUDE.md, tasks/EP03_kb.md и вкладку 2 из docs/EMMA_PANEL_TZ_v2.md (раздел 3). Реализуй все задачи из task-файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из tasks/EP03_kb.md и покажи результаты, включая живой смоук: загрузи тестовый TXT с уникальным фактом и убедись, что Эмма отвечает по нему. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4, и что старые тесты не сломаны (go test -p 1). Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку одним squash-коммитом с сообщением EP-03: emma panel — knowledge base upload, pdf text, async rag indexing и запушь ветку на GitHub.»

> Что получите после EP-03: файлы базы знаний (TXT, MD, PDF) загружаются
> через API, автоматически режутся и попадают в поисковый индекс — Эмма
> отвечает по ним без правки кода. Статусы индексации, переиндексация,
> удаление с вычисткой. Сканы PDF без текста не поддерживаются (честная
> ошибка). Интерфейс — в EP-07.

---

## EP-04 — Панель Эммы: файлы для отправки + маркер-протокол

«Создай git-ветку feat/ep04-files от feat/m11-ops и переключись на неё.»

«Работаем над эпиком EP-04 панели управления Эммой. Прочитай CLAUDE.md, tasks/EP04_files.md и вкладку 3 из docs/EMMA_PANEL_TZ_v2.md (раздел 3). Реализуй все задачи из task-файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из tasks/EP04_files.md и покажи результаты, включая живой смоук: загрузи PDF с описанием "отправь, когда клиент спрашивает цены", спроси у Эммы про цены и убедись, что файл пришёл. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4, и что старые тесты не сломаны (go test -p 1). Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку одним squash-коммитом с сообщением EP-04: emma panel — send files library, marker protocol, telegram attachments и запушь ветку на GitHub.»

> Что получите после EP-04: Эмма сама отправляет клиентам прайсы, брошюры
> и фото — когда сочтёт уместным по описанию файла. Отправленный файл
> виден менеджеру в чате карточки. Управление библиотекой — через API,
> интерфейс появится в EP-07.

---

## EP-05 — Панель Эммы: контакты, /start, кнопка менеджера, handoff

«Создай git-ветку feat/ep05-contacts-scenario от feat/m11-ops и переключись на неё.»

«Работаем над эпиком EP-05 панели управления Эммой. Прочитай CLAUDE.md, tasks/EP05_contacts_scenario.md, вкладки 4–5 и раздел 4 из docs/EMMA_PANEL_TZ_v2.md. Реализуй все задачи из task-файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из tasks/EP05_contacts_scenario.md и покажи результаты, включая живой смоук: попроси у Эммы менеджера фразой и кнопкой, убедись что диалог перешёл менеджеру и пришло уведомление. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4, и что старые тесты не сломаны (go test -p 1). Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку одним squash-коммитом с сообщением EP-05: emma panel — contacts, welcome, manager button, client handoff и запушь ветку на GitHub.»

> Что получите после EP-05: клиент может позвать живого менеджера кнопкой
> или фразой — Эмма замолкает, менеджеру уходит уведомление, карточка
> подсвечивается (если менеджер молчит 20 минут — Эмма вежливо подхватит).
> Плюс справочник контактов в контексте Эммы и настраиваемое приветствие
> на /start. По умолчанию всё выключено — поведение Эммы не меняется,
> пока вы не заполните поля в панели (интерфейс — EP-07).

---

## EP-06 — Панель Эммы: статистика, расходы Claude, алерты

«Создай git-ветку feat/ep06-stats от feat/m11-ops и переключись на неё.»

«Работаем над эпиком EP-06 панели управления Эммой. Прочитай CLAUDE.md, tasks/EP06_stats.md и вкладку 6 из docs/EMMA_PANEL_TZ_v2.md (раздел 3). Реализуй все задачи из task-файла, строго соблюдай жёсткие правила из CLAUDE.md §4, доведи до прохождения всех критериев приёмки. По ходу объясняй простыми словами, что делаешь.»

«Прогони все критерии приёмки из tasks/EP06_stats.md и покажи результаты, включая живой смоук: напиши Эмме пару сообщений, проверь что статистика показывает ответы и стоимость, и отправь пробный алерт себе в Telegram. Что не проходит — доделай.»

«Проверь, что не нарушено ни одно жёсткое правило из CLAUDE.md §4, и что старые тесты не сломаны (go test -p 1). Пройдись по списку и подтверди по каждому пункту.»

«Сделай сборку и проверку кода (go build ./..., go vet ./...), убедись что чисто. Затем закоммить изменения в текущую ветку одним squash-коммитом с сообщением EP-06: emma panel — stats, claude cost tracking, error journal, telegram alerts и запушь ветку на GitHub.»

> Что получите после EP-06: полная статистика Эммы по периодам — диалоги,
> скорость ответов, отправленные файлы, handoff'ы, потраченные токены и
> примерная стоимость в долларах. Журнал ошибок с фильтрами. Алерты в ваш
> Telegram при серии сбоев (настраивается chat_id, есть кнопка пробного
> алерта). Останется один эпик — EP-07, интерфейс.

---

## Полезные команды на любой случай

Если застряло:
«Что-то не работает: <опиши что видишь>. Разберись в причине, объясни просто и почини. Не переходи дальше, пока это не заработает.»

Если тест падает:
«Тест <название> падает. Покажи ошибку, объясни причину и исправь. Не отключай тест ради галочки — почини по-настоящему.»

Если непонятно, что происходит:
«Объясни как нетехническому человеку: что ты сейчас сделал и зачем.»

Откатить неудачное:
«Откати незакоммиченные изменения к последнему коммиту.»

Проверить готовый эпик отдельно (в новом чате):
«Проведи AQ-ревью этого эпика: прочитай его task-файл и код, найди расхождения со спецификацией и потенциальные баги.»

-- 0021 down: no-op намеренно (task EP-02 §4) — данные не откатываем:
-- к моменту down в таблице могут жить версии владельца, а отличить сид
-- от сохранённой из панели версии после правок нельзя. Пустая таблица
-- не нужна и для кода: воркер при ней падает на константу-fallback.
SELECT 1;

-- 0001_init (M0): намеренно пустая миграция-плейсхолдер.
-- Задаёт паттерн NNNN_name.up.sql / .down.sql (SRS §8.6).
-- Схема данных (leads, messages, payment_events, rag_audit, lgpd_audit)
-- появится в M1 отдельными миграциями.
SELECT 1;
